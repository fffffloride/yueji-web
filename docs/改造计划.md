# 悦己 DLumière 微信小程序改造计划

> 总计划在服务端仓库：`E:\CODE\youlai-nest\docs\改造计划.md`  
> 管理端：`E:\CODE\vue3-element-admin\docs\改造计划.md`  
> 阶段编号三端共用。换工具后从本文「当前进度」接着做。

## 当前进度（2026-08-17）

| 阶段 | 状态 |
|------|------|
| 脚手架初始化 | 已完成（uni-app Vue3 + wot-design-uni + 全量路由空壳） |
| 2 产品中心对接 | 代码已写（分类/列表/搜索/详情）；**请确认是否已 git commit** |
| 3 购物车 + 下单 | **下一步**（等服务端 + 管理端阶段 3） |
| 4 支付结果 + 个人中心 | 未开始 |
| 5–9 | 未开始 |

## 项目要点

- 路径：`E:\CODE\yueji-web`
- 技术栈：uni-app + Vue3 + TS + Vite + Pinia + wot-design-uni + UnoCSS(applet)
- 环境：`.env.development` → `VITE_API_BASE_URL=http://localhost:8000`，`VITE_USE_MOCK=false`
- 请求封装：`src/utils/request.ts`，成功码 `"00000"`，消息字段 `msg`（已对齐 youlai-nest）
- 路由常量：`src/constants/routes.ts`；跳转用 `navigate()`
- 金额展示：`formatPrice(cents, true)`（分 → 元）

## 阶段 2 已落地（产品中心）

- API 防腐：`src/api/product/index.ts` 对接 `/api/v1/app/product/*`，映射 `mainImage→cover`、`album→banners`、`detail→detailHtml`
- Tab：`src/pages/product/index.vue`（左侧分类 + 排序 + 触底加载）
- 分包：搜索 / 列表 / 详情（`src/pages-sub/product/`）
- 卡片组件：`src/components/YjProductCard.vue`
- 详情页加购/购买仍是占位 toast，阶段 3 接真实接口

C 端登录（`src/api/user`）路径尚未对齐后端 `/api/v1/app/auth/*` 与 `/api/v1/app/member/profile`，阶段 4 个人中心时一并改。

## 阶段 3 小程序任务（服务端与管理端完成后再做）

- 购物车页（`src/pages/cart/index.vue`，现为占位）
- 订单确认 / 我的订单列表 / 详情
- 商品详情「加入购物车 / 立即购买」接到真实接口
- 开发联调可用后端 `POST /api/v1/app/auth/mock-login`

## 开发纪律

- 等服务端接口可用、管理端能录出真实商品/订单后再改页面，不要用假数据把空壳填满后无法替换。
- 回复使用简体中文。
